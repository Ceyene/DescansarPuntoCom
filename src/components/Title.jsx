//dependencies
import React from "react";

//Title: it returns an h1 with the text: "Descansar.com"
function Title({ appName }) {
  return <h1 className="title is-1">{appName}</h1>;
}

export default Title;
